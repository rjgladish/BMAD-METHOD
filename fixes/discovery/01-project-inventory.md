# Project Inventory — BMAD Customization Audit

Read-only snapshot, 2026-04-29. Inspected projects from the metaprompt P-1 list.
Worktrees other than the primary working tree (per `git worktree list`) were skipped.
The DUBIOUS list (linkml `agent-*` worktrees, kai `substrate.wt/`, kai `issue-claudefat`) was excluded as instructed.

mtime pre-filter rule: `_bmad/` files newer than the newest `_bmad-output/` artifact = customization candidates. Empty result = pristine vendored install.

---

### architecture
- canonical_root: `/home/randyg/repos/architecture`
- bmad_install_shape: full `_bmad/` (with `_memory/` sidecar) + tracked artifacts subset of `_bmad-output/`
- bmad_tracked_in_git: `_bmad/` no (`.gitignore` line 6: `_bmad`); `_bmad-output/` partial (5 files tracked; 1 deletion staged)
- mtime_pre_filter: pristine — newest `_bmad/` mtime 1773860470 (2026-03-15) is older than newest `_bmad-output/` mtime 1774815038 (2026-03-29). User flagged architecture as always-inspect anyway.
- customization_files:
  - `_bmad/_memory/tech-writer-sidecar/documentation-standards.md` (5.7KB) — CommonMark + style rules; **byte-identical to Factweave's copy**, so this is a BMAD `_memory` default install artifact, not a project-local customization
  - `bmad` symlink → `_bmad` (Apr 29) — local convenience, not a BMAD edit
  - `_bmad-output/planning-artifacts/extracted-yaml-provenance.md` (tracked) — explains the AsciiDoc→YAML extraction pipeline used by this project
- ad_hoc_workarounds: 0 in `_bmad-output/` (rg over WORKAROUND/HACK/TODO(BMAD)/FIXME/HOTFIX/"if exists" returned nothing)
- utility_scripts (PROMOTION CANDIDATES — substrate-tier, D1/D3):
  - `/home/randyg/repos/architecture/elt/extract-section.sh` (1.9KB, Mar 18) — extracts a single `[#{idprefix}<section-id>]` block from a `.adoc` file to its next anchor; used to re-feed sections into BMAD workflows
  - `/home/randyg/repos/architecture/tools/parsers/parse-uc.awk` (4.9KB, Mar 28) — Use Case (UC-*) AsciiDoc→YAML parser
  - `/home/randyg/repos/architecture/tools/parsers/parse-us.awk` (4.5KB, Mar 28) — User Story (US-*) AsciiDoc→YAML parser (extracts FRs, ACs, narrative)
  - `/home/randyg/repos/architecture/tools/parsers/parse-ss.awk` (1.6KB, Mar 27) — Subsystem (SS-*) parser
  - `/home/randyg/repos/architecture/tools/parsers/parse-dm.awk` (3.5KB, Mar 28) — Data Model (DM-*) parser
  - `/home/randyg/repos/architecture/elt/conops.adoc-index`, `data-models.adoc-index` (Mar 18) — section-anchor indexes generated by these tools
  - Targets: `/home/randyg/repos/architecture/elt/extracted/elt-{data-models,subsystems,use-cases,user-stories}.yaml` and `fw/extracted/fwa-{subsystems,use-cases,user-stories}.yaml`
- last_bmad_activity: 2026-03-29 (last commit touching `_bmad-output/`)
- worktrees: 1 (primary `/home/randyg/repos/architecture` on branch `elt/prd-element-lifecycle`)

### archbox (kai/substrate)
- canonical_root: `/home/randyg/repos/kai/substrate` (not a standalone repo; project lives at `Projects/archbox/`)
- bmad_install_shape: full `_bmad/` + full `_bmad-output/` both **tracked in git**
- bmad_tracked_in_git: yes (233 `_bmad/` files, 221 `_bmad-output/` files)
- mtime_pre_filter: pristine — newest `_bmad/` mtime 1775090027 (2026-04-04) older than newest `_bmad-output/` mtime 1775687226 (2026-04-10)
- customization_files:
  - `_bmad-output/liaison/README.md` (2.5KB, Apr 3) — defines a cross-project "liaison" mailbox protocol (`incoming/`, `outgoing/`, `archived/`); NOT a canonical BMAD concept
  - `_bmad-output/liaison/incoming/*` (5 files) — claudebox source/architecture/validation findings shipped from another project
  - `_bmad-output/architecture/` — 8+ files of arch artifacts (`archbox-box.md`, `archbox-manifest.md`, `index.md`, subdirs `class/`, `enum/`, `slot/`, `subset/`, `type/`, `isolated-mechanism-deployment-architecture.md` 28KB) — looks like a custom architecture-doc layout, NOT canonical BMAD `_bmad-output/` shape
  - `_bmad-output/implementation-artifacts/sprint-status.yaml` (Mar 8) — appears canonical-style; M (modified) in current status
  - All `_bmad/_config/agents/*.customize.yaml` are 41-line empty boilerplate (not customization)
  - `_bmad/_memory/tech-writer-sidecar/documentation-standards.md` — same default file
- ad_hoc_workarounds: 0 BMAD-specific. 6 hits matching "if exists" but all in `claudebox-*` domain content (filesystem mount semantics — not a BMAD-process workaround).
- utility_scripts: none specific to archbox/BMAD found at `--max-depth 4`
- last_bmad_activity: 2026-04-08 (commit on `_bmad/`/`_bmad-output/`)
- worktrees: 3 (kai repo: `substrate` primary, `kai/issue-claudefat`, `kai/substrate.wt/cbp-schema` — only primary inspected)

### Factweave (kai/substrate)
- canonical_root: `/home/randyg/repos/kai/substrate` (project at `Plans/Factweave/`)
- bmad_install_shape: full `_bmad/` (gitignored) + slim `_bmad-output/` (only `planning-artifacts/`, `implementation-artifacts/`)
- bmad_tracked_in_git: `_bmad/` no (0 files); `_bmad-output/` partial (8 files in `planning-artifacts/`)
- mtime_pre_filter: pristine — newest `_bmad/` mtime 1773364193 (2026-03-09) older than newest `_bmad-output/` mtime 1775326270 (2026-04-06)
- customization_files:
  - Tracked outputs include `FR-REWRITE-INSTRUCTIONS.md`, `FactWeave FR Reviewed.csv`, `factweave-fr-reconciliation.csv`/`.tsv`, `prd-validation-report.md`, both `PRD.md` and `prd.md` (case duplicate) — domain content, NOT BMAD process customization
  - `_bmad/_memory/tech-writer-sidecar/documentation-standards.md` — same default
  - The `bmad-create-product-brief`, `bmad-brainstorming`, etc. canonical workflow names (vs `create-product-brief` in linkml/archbox) suggest a DIFFERENT BMAD install version than archbox/linkml — Factweave matches the architecture install pattern
- ad_hoc_workarounds: not enumerated (output mostly markdown/csv content artifacts)
- utility_scripts: none
- last_bmad_activity: 2026-03-23
- worktrees: shares `kai/substrate` worktrees (3 listed)

### linkml
- canonical_root: `/home/randyg/repos/linkml`
- bmad_install_shape: full `_bmad/` + extensive `_bmad-output/` with custom subdirs; **both tracked**
- bmad_tracked_in_git: yes (233 `_bmad/`, 211 `_bmad-output/`)
- mtime_pre_filter: pristine — newest `_bmad/` mtime 1774982114 (2026-03-31) older than newest `_bmad-output/` mtime 1777400324 (2026-04-28). The 2026-03-31 boundary corresponds to the evolution-2 patch commit.
- customization_files:
  - **Evolution-2 already applied as a tracked commit** (`d5dbbdb0a fix(bmad): evolution-2 — workflow state recovery gate`, 12 files +714/-2, Mar 31) — patches `step-01b-continue.md` etc. across `bmm/workflows/` and `core/workflows/brainstorming/`
  - `_bmad-output/liaison/{incoming,outgoing}/` — same cross-project mailbox protocol as archbox; 7 incoming files (antora research, baml studies, archbox responses, lkml-rs analysis)
  - `_bmad-output/failings/` — 5 files (synopsis.txt, messy-agents-session.txt, fourth-layer-violation-audit.txt, another-bmad-ADR-screwup-wtf.txt, 3f5555b9-…-messy-agents-fat.txt) — narrative of architectural failures captured for retrospection; NOT canonical BMAD
  - `_bmad-output/learnings/` — 5 files including **`draft-bmad-improvement-analysis.md`** (BMAD upstream-contribution prep, 2026-03-25) and `bmad-upstream-contribution-strategy.md` — these directly enumerate BMAD deficiencies and proposed fixes
  - `_bmad-output/config.yaml` — project-level BMAD config (not in `_bmad/_memory/`); points `paths.docs` at `lkml/docs`, `paths.stories` at `_bmad-output/implementation-artifacts`, custom `bmm.workflow_status_file: docs/bmm-workflow-status.yaml`
  - `_bmad-output/planning-artifacts/extracted/lkml-{actors,data-models,subsystems,use-cases,user-stories}.yaml` + `requirements-metamodel.yaml` (Mar 27) — same AsciiDoc→YAML pattern as architecture; mechanically extracted
  - **`lkml-gen/src/lkml_gen/generators/templates/{actor,conops,data-model,subsystem,use-case,user-story}-{jinja2,mustache}.adoc`** — 12 templates promoted into a Python package — these are the formal-shape templates derived from the requirements-metamodel; STRONG D-tier substrate candidates
  - All `_bmad/_config/agents/*.customize.yaml` are 41-line boilerplate
- ad_hoc_workarounds: 0 in `_bmad-output/` (no WORKAROUND/HACK/etc. hits)
- utility_scripts:
  - `lkml-architecture.py` — auto-generated Pydantic models from a LinkML schema for arch-conformance testing; not BMAD
  - `lkml-cli/scripts/categorize-file.sh` — domain script; not BMAD
- last_bmad_activity: 2026-04-28 (last commit on `_bmad-output/`); evolution-2 patch 2026-03-31
- worktrees: 7 (1 primary + 6 locked `agent-*` — DUBIOUS list, all skipped per instructions)

### padimae (kai/substrate)
- canonical_root: `/home/randyg/repos/kai/substrate` (project at `Plans/padimae/`)
- bmad_install_shape: full `_bmad/` (tracked) + minimal `_bmad-output/` (single `planning-artifacts/` file tracked)
- bmad_tracked_in_git: `_bmad/` yes (232 files); `_bmad-output/` partial (1 file)
- mtime_pre_filter: pristine — newest `_bmad/` 1771902288 (2026-02-22) older than newest `_bmad-output/` 1771905601 (2026-02-22)
- customization_files: none. Single commit `6dff3d8 feat(padimae): BMAD v6 installed, architecture inputs drafted from verified knowns` — fresh install, no project-local edits
- ad_hoc_workarounds: 0
- utility_scripts: none
- last_bmad_activity: 2026-02-23
- worktrees: shares kai/substrate worktrees

### describe (kai/substrate)
- canonical_root: `/home/randyg/repos/kai/substrate` (project at `Projects/describe/`)
- bmad_install_shape: full `_bmad/` + full `_bmad-output/` (both gitignored; 0 tracked)
- bmad_tracked_in_git: no
- mtime_pre_filter: pristine — newest `_bmad/` 1772514091 older than `_bmad-output/` 1773077295
- customization_files: none surfaced; install entirely untracked. Output dirs: `bmb-creations/`, `implementation-artifacts/`, `planning-artifacts/`, `test-artifacts/` — canonical layout
- ad_hoc_workarounds: not inspected (vendored, untracked, low signal)
- utility_scripts: none
- last_bmad_activity: 2026-03-03 (mtime; not in git)
- worktrees: shares kai/substrate worktrees

### claudefat (kai/substrate)
- canonical_root: `/home/randyg/repos/kai/substrate` (project at `Projects/claudefat/`)
- bmad_install_shape: full `_bmad/` (gitignored) + slim `_bmad-output/` (2 tracked artifacts)
- bmad_tracked_in_git: `_bmad/` no; `_bmad-output/` partial (2 files)
- mtime_pre_filter: pristine — newest `_bmad/` 1772663484 older than `_bmad-output/` 1773179480
- customization_files: none
- ad_hoc_workarounds: not inspected
- utility_scripts: none
- last_bmad_activity: 2026-03-12
- worktrees: shares kai/substrate worktrees

### teague
- canonical_root: `/home/randyg/repos/teague`
- bmad_install_shape: full `_bmad/` (gitignored) + empty `_bmad-output/` (no artifact files at any depth)
- bmad_tracked_in_git: no (0 files)
- mtime_pre_filter: inapplicable — `_bmad-output/` empty. `_bmad/` newest mtime 1773844148 (2026-03-18). Install only, no workflow runs.
- customization_files: none
- ad_hoc_workarounds: 0
- utility_scripts: domain (`util/plank.bash`, `src/`); none touch BMAD
- last_bmad_activity: 2026-03-18 (mtime)
- worktrees: 1

### relloq
- canonical_root: `/home/randyg/repos/relloq`
- bmad_install_shape: full `_bmad/` (gitignored) + slim `_bmad-output/` (1 file tracked)
- bmad_tracked_in_git: `_bmad/` no; `_bmad-output/` partial (1 file)
- mtime_pre_filter: pristine — newest `_bmad/` 1772859130 older than `_bmad-output/` 1773084805
- customization_files: none
- ad_hoc_workarounds: 0
- utility_scripts: none touch BMAD
- last_bmad_activity: 2026-03-09
- worktrees: 1

---

## Summary — Customization Intensity Ranking

| Rank | Project | Intensity | Drivers |
|:-:|:--|:--:|:--|
| 1 | **linkml** | high | evolution-2 patch already applied; `liaison/`, `failings/`, `learnings/` custom output dirs; `extracted/*.yaml` + lkml-gen `{jinja2,mustache}.adoc` template package; explicit `draft-bmad-improvement-analysis.md` upstream prep doc |
| 2 | **architecture** | high | substrate-tier AsciiDoc→YAML tooling (`tools/parsers/parse-{uc,us,ss,dm}.awk`, `elt/extract-section.sh`, `*.adoc-index` indexes, `elt/extracted/*.yaml`, `fw/extracted/*.yaml`); known D1/D3 (markdown overhang) site per metaprompt |
| 3 | **archbox** | medium | `_bmad-output/liaison/` mailbox protocol; non-canonical `_bmad-output/architecture/` layout (manifest, box, class/enum/slot/subset/type subdirs); fully tracked install |
| 4 | **Factweave** | low | content-only customization (PRD.md/prd.md case-pair, FR rewrite/reconciliation files); BMAD process otherwise pristine |
| 5 | **padimae / describe / claudefat / teague / relloq** | none | pristine vendored installs; no process customization, no output-side workarounds |

**Cross-cutting findings (not in single-project rows):**
- The cross-project `liaison/` mailbox protocol exists in BOTH archbox and linkml with identical schema — this is a candidate for promotion to canonical BMAD as a multi-project coordination primitive (taxonomy A4 / cross-project).
- The AsciiDoc→YAML extraction toolchain exists in BOTH architecture (canonical owner: `tools/parsers/`) and is consumed by linkml (`_bmad-output/planning-artifacts/extracted/*.yaml` produced 1 day later than architecture's). Substrate-tier D-category candidate.
- The Jinja2/Mustache AsciiDoc generator templates in `linkml/lkml-gen/src/lkml_gen/generators/templates/` round-trip the requirements-metamodel back into AsciiDoc — this is the "shape" half of the D-tier story (extraction + emission).
- Architecture and Factweave use OLDER BMAD install version (workflow dirs prefixed `bmad-`); archbox/linkml use NEWER version (no prefix). evolution-2 step-01b-continue.md is present in BOTH variants, suggesting upstream BMAD already absorbed it OR vendored installs were patched uniformly. Confirmation: linkml's tracked patch commit `d5dbbdb0a` shows the diff is real and explicitly cites `~/repos/harness/skills/BMAD-METHOD.git/fixes/evolution-2/`.
